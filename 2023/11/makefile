#test1.o:test1.c
#	gcc -c test1.c -o test1.o
#test2.o:test2.c
#	gcc -c test2.c -o test2.o


#有隐含规则.c到.o

#
#   $@，表示规则中的目标文件名集合，如果存在多个目标文件，$@则表示其中每一个文件名。助记，’@’
#很像是 at，aim at，表示瞄准目标。
#$<，表示规则中依赖文件中的第 1 个文件。助记，
#‘<’很像是集合的最左边，也就是第 1 个。
#$^，表示规则中所有依赖文件的集合，如果集合中有重复的文件，$^会自动去重。助记，’^’很像从上往下
#罩的动作，能罩住很大的范围，所以称为集合。
#$?，表示规则中，所有比目标文件 mtime 更新的依赖文件集合。助记，’?’表示疑问，make 最大的疑
#问就是依赖文件的 mtime 是否比目标文件的 mtime 要新。
#  
#  $@ 所有目标文件     $< 依赖文件里的·第一个   $^ 所有依赖文件   $? 

%.o:%.c
	gcc -c -o $@ $^
test.bin:test1.o test2.o
	gcc	-o $@ $^
all:test.bin
	@echo "OK"

clean:
	rm -rf *.o *.bin